/** Ground type for each grid cell. Rendered by core as a biome texture or camp clearing. */
export enum Cell {
  Meadows,
  ScorchedWastes,
  CrimsonGrove,
  FungalMire,
  Frostbane,
  Water,
  Camp,
}
